'use strict'

const header = require('./src/header')
const tileIndex = require('./src/tileIndex')
const tiles = require('./src/tiles')

module.exports = {
    header: header,
    tileIndex: tileIndex,
    tiles: tiles
}